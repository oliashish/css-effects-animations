import "../styles/GradientMovement.css";

const GradientMovement = () => {
    return (
        <div className="effect-container">
            <h1>This is Gradient movement effect happing in the background</h1>
        </div>
    );
};

export default GradientMovement;
